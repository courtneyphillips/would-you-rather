<h1>All Bouts:</h1>

<ul id="bouts">
  <% @bouts.each do |bout| %>
   Bout <%= bout.id %>:
    <% bout.options.each do |option| %>
      <li class="option"><%= option.name %></li>
    <% end %>
  <% end %>
</ul>
<%= link_to "New Bout", new_bout_path, id: "new_bout_link", remote: true %>
